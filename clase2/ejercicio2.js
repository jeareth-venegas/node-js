/*Desarrolle un programa que permita leer tres valores y almacenarlos en las
variables A, B y C respectivamente. El algoritmo debe determinar cual es el
mayor.*/

function numeromayor ( a, b, c){
return Math.max( a, b, c);
}